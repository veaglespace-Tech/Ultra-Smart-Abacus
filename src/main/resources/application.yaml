spring:
  application:
    name: Abacus

  datasource:
    url: jdbc:mysql://localhost:3306/abacus_db?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8080